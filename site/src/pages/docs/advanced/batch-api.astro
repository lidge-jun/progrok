---
import DocsLayout from '../../../layouts/DocsLayout.astro';
const title = 'Batch API';
---

<DocsLayout title={title}>
<h1>Batch API</h1>
<p>The Batch API enables async bulk processing of requests at a 20–50% discount compared to real-time pricing. Submit a batch via <code>POST /v1/batches</code> with a JSONL file of requests and poll for completion.</p>
</DocsLayout>

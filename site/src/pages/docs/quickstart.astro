---
import DocsLayout from '../../layouts/DocsLayout.astro';
const title = 'Quick Start';
---

<DocsLayout title={title}>
<h1>Quick Start</h1>

<h2>1. Install</h2>
<pre><code>{`npm install -g progrok`}</code></pre>

<h2>2. Login</h2>
<pre><code>{`progrok login`}</code></pre>
<p>Opens your browser to authenticate with xAI via OAuth.</p>

<h2>3. Start the proxy</h2>
<pre><code>{`progrok proxy`}</code></pre>
<p>Starts a local OpenAI-compatible API server on port 18645.</p>

<h2>4. Make a request</h2>
<pre><code>{`curl http://localhost:18645/v1/chat/completions \\
  -H "Content-Type: application/json" \\
  -d '{"model": "grok-4.3", "messages": [{"role": "user", "content": "Hello!"}]}'`}</code></pre>
</DocsLayout>

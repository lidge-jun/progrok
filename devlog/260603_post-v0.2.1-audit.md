# Post-v0.2.1 Security & Quality Audit

Date: 2026-06-03

## Scope

All 5 commits since v0.2.1 release, covering:
- `src/commands/billing.ts` (NEW — 100 lines)
- `src/commands/models.ts` (tag detection update)
- `src/index.ts` (billing registration)
- `README.md`, `skills/progrok/SKILL.md` (grok-composer docs)

## Audit Dimensions (4 parallel sub-agents)

### 1. Security

| Severity | Finding | Status |
|----------|---------|--------|
| MEDIUM | Unvalidated API response shape → uncaught TypeError crash | FIXED |
| LOW | `.json()` parse failure unhandled on non-JSON 200 response | FIXED |

### 2. Code Quality

| Type | Finding | Status |
|------|---------|--------|
| BUG | Crash on missing `.config` property in billing response | FIXED |
| BUG | Crash on null `billingPeriodStart`/`billingPeriodEnd` | FIXED |
| ROBUSTNESS | No try/catch around billing action handler | FIXED |
| EDGE_CASE | `formatPrice` treats 0 as falsy → shows "-" instead of "$0.00" | FIXED |
| ROBUSTNESS | Crash if `data.models`/`data.data` is undefined | FIXED |

### 3. Documentation

| Item | Status |
|------|--------|
| README commands table — billing | FIXED |
| SKILL.md CLI reference — billing | FIXED |
| capabilities.ts commands array — billing | FIXED |
| capabilities.ts models — grok-composer-2.5/fast | FIXED |

### 4. Build/Config

All 4 checks PASS (dependencies, TS compilation, peer deps, full build).

## Patches Applied

### billing.ts
- Wrapped entire action handler in try/catch (matches models.ts pattern)
- Added runtime validation: `raw?.config`, `monthlyLimit?.val == null`, `used?.val == null`
- Added null-safe period date extraction: `billing.billingPeriodStart ?? "unknown"`
- Added inner try/catch for user JSON parse
- Added `?? []` fallback for history in JSON output

### models.ts
- Changed `if (!ticks)` to `if (ticks == null)` in `formatPrice`
- Added `?? []` defensive fallback for `data.models` and `data.data` iteration

### capabilities.ts
- Added `"billing"` to commands array
- Added `grok-composer-2.5-fast` and `grok-composer-2.5` model entries with limitations

### README.md
- Added `progrok billing` and `progrok billing --json` to Commands table

### SKILL.md
- Added billing commands to Dynamic Metadata section

## Verification

- `npx tsc --noEmit` — PASS (0 errors)
- `npm run build` — PASS (73.86 KB bundle)
- Employee (Backend) read-only verification — DONE (all clear)
